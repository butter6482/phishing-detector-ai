# Runtime del backend FastAPI. Fuente única de dependencias de ejecución.
fastapi==0.115.*
uvicorn[standard]==0.30.*
pydantic>=2.8,<3
python-dotenv>=1.0,<2
httpx>=0.27,<0.28
joblib>=1.3,<2
# fijado a la versión con la que se entrenó modelo_entrenado.pkl (evita InconsistentVersionWarning)
scikit-learn==1.7.*
python-multipart>=0.0.9,<0.1

# OCR (src/ocr.py) no está cableado a ningún endpoint todavía. Si se habilita,
# añadir aquí y el binario `tesseract-ocr` en el Dockerfile:
#   pillow>=10.3,<12
#   pytesseract>=0.3.10,<0.4
