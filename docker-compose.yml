# Imagen todo-en-uno: build del frontend + backend FastAPI tras nginx (ver ./Dockerfile).
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: phishing-detector
    container_name: phishing-detector
    ports:
      - "8080:80"
    env_file:
      - .env
    restart: unless-stopped
